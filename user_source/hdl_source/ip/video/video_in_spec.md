# `video_in` 模块规格

## DDR 写请求

- `O_ddr_user_req` 表示有写突发正在进行，或输入 FIFO 已具备新写突发条件。
- `I_ddr_user_ready` 是顶层突发仲裁授权和 DDR FIFO 可接受状态的合取结果。
- 写突发只在获得授权后启动；突发中断时保持原有计数和地址。

## 帧错误

- `I_mipi_rx_error` 在 DDR 帧开始时决定当前写指针是否回退到上一帧缓冲区。
- 该信号必须由顶层完成跨时钟同步后输入。
